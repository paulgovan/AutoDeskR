% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{waitForWorkItem}
\alias{waitForWorkItem}
\title{Wait for a Design Automation WorkItem to Complete.}
\usage{
waitForWorkItem(id, token, interval = 5, timeout = 300, verbose = TRUE)
}
\arguments{
\item{id}{A string. WorkItem ID from \code{makePdf()$content$id}.}

\item{token}{A string or \code{aps_token} object with \code{code:all} scope.}

\item{interval}{Seconds between polls. Defaults to \code{5}.}

\item{timeout}{Maximum seconds to wait before aborting. Defaults to
\code{300}.}

\item{verbose}{If \code{TRUE} (default), prints a message after each poll.}
}
\value{
The final \code{\link{checkPdf}} response object.
}
\description{
Polls \code{\link{checkPdf}} at a fixed interval until the WorkItem reaches
a terminal state (any status other than \code{"inprogress"} or
\code{"pending"}).
}
\examples{
\dontrun{
resp <- makePdf(source = mySource, destination = myDest, token = myToken)
done <- waitForWorkItem(id = resp$content$id, token = myToken)
done$content$status
}
}
