% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{waitForFile}
\alias{waitForFile}
\title{Wait for a Model Derivative Translation to Complete.}
\usage{
waitForFile(urn, token, interval = 5, timeout = 300, verbose = TRUE)
}
\arguments{
\item{urn}{A string. Base64-encoded source URN.}

\item{token}{A string or \code{aps_token} object with \code{data:read} scope.}

\item{interval}{Seconds between polls. Defaults to \code{5}.}

\item{timeout}{Maximum seconds to wait before aborting. Defaults to
\code{300}.}

\item{verbose}{If \code{TRUE} (default), prints a message after each poll.}
}
\value{
The final \code{\link{checkFile}} response object.
}
\description{
Polls \code{\link{checkFile}} at a fixed interval until the translation
reaches a terminal state (\code{"success"}, \code{"failed"}, or
\code{"timeout"}).
}
\examples{
\dontrun{
myEncodedUrn <- jsonlite::base64_enc(myUrn)
resp <- translateSvf(urn = myEncodedUrn, token = myToken)
done <- waitForFile(urn = myEncodedUrn, token = myToken)
done$content$status
}
}
