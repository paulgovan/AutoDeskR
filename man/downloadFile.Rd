% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modelDerivative.R
\name{downloadFile}
\alias{downloadFile}
\title{Download a file locally.}
\usage{
downloadFile(urn = NULL, output_urn = NULL, token = NULL, destfile = NULL)
}
\arguments{
\item{urn}{A string. Source URN (objectId) for the file. Note the URN must be
Base64 encoded. To encode the URN, see, for example, the
\code{jsonlite::base64_enc} function.}

\item{output_urn}{A string. Output URN retrieved via \code{\link{getOutputUrn}}.}

\item{token}{A string. Token generated with \code{\link{getToken}} function
with \code{data:read} and \code{data:write} scopes.}

\item{destfile}{A string. Local file path to save binary responses (e.g.
downloaded geometry files). When \code{NULL} and the response is not JSON,
a warning is issued and the raw bytes are returned.}
}
\value{
An object containing either parsed JSON content or, for binary
  responses, the path to the saved file.
}
\description{
Download a file from the AutoDesk Platform Services using the Model Derivative API.
}
\examples{
\dontrun{
# Download the "aerial.dwg" obj file
myEncodedOutputUrn <- jsonlite::base64_enc(myOutputUrn)
resp <- downloadFile(urn <- myEncodedUrn, output_urn <- myEncodedOutputUrn,
           token = myToken, destfile = "aerial.obj")
}
}
